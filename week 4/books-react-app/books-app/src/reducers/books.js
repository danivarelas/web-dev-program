const books = (state = [], actions) => {
    switch(actions.type){
        // crear un objeto nuevo desde cero (inmutable)
        case "SELECT_BOOK":
        default:
            return state;
    }
}

export default books;